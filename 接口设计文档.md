# 📝 闲置物品交易平台接口设计文档

## 1. 用户模块接口

### 1.1 认证相关

| 接口名称     | URL                  | 方法 | 描述          | 请求参数              | 返回示例                            |
| ------------ | -------------------- | ---- | ------------- | --------------------  | ----------------------------------- |
| 用户注册     | `/api/auth/register` | POST | 支持用户名注册    | `RegisterRequest`     | `{ "code": 200, "msg":"成功注册" }`                  |
| 用户登录     | `/api/auth/login`    | POST | JWT鉴权登录   | `LoginRequest`        | `{ "code": 200, "data":{"username":"", "nickname":"", "email":"", "avatar":"", "token":"xxx"}}`  |
| 用户登出     | `/api/auth/logout`   | POST | Token加入黑名单，安全退出  | 无       | `{ "code": 200, "msg":"成功登出" }`                  |

### 1.2 用户信息管理

| 接口名称     | URL                  | 方法 | 描述          | 请求参数              | 返回示例                            |
| ------------ | -------------------- | ---- | ------------- | --------------------  | ----------------------------------- |
| 修改个人信息 | `/api/user/info`     | PUT  | 修改用户昵称/邮箱/电话  | `UserInfo`            | `{ "code": 200, "msg":"个人信息修改成功" }`                   |
| 修改密码     | `/api/user/password` | PUT  | 原密码验证后修改新密码  | `UserPassword`        | `{ "code": 200, "msg":"密码修改成功，请重新登录" }`                  |
| 上传头像     | `/api/user/avatar`   | PUT  | 上传用户头像(文件上传)  | `avatar(multipart)`       | `{ "code": 200, "data":"头像URL"}`                  |
| 获取用户信息 | `/api/user/info`     | GET  | 获取当前登录用户信息  | 无       | `{ "code": 200, "data":{"nickname":"", "email":"", "avatar":""}}`                  |

### 1.3 卖家信息查看

| 接口名称     | URL                  | 方法 | 描述          | 请求参数              | 返回示例                            |
| ------------ | -------------------- | ---- | ------------- | --------------------  | ----------------------------------- |
| 查看卖家信息 | `/api/user/seller/{sellerId}` | GET  | 查看卖家昵称、头像、信用信息  | `sellerId`       | `{ "code": 200, "data":{"nickname":"", "avatar":"", "soldCount": 0, "avgRating": 4.5}}`                  |

---

## 2. 商品模块接口

### 2.1 卖家商品管理

| 接口名称      | URL                                            | 方法   | 描述           | 请求参数              | 返回示例                                      |
| ------------- | ---------------------------------------------- | ------ | -------------- | --------------------- | --------------------------------------------- |
| 发布商品      | `/api/product/publish`                         | POST   | 发布新商品       | `ProductPublishDto`   | `{ "code": 200, "data": 123 }`               |
| 编辑商品信息  | `/api/product/changeInfo/{productId}`          | PUT    | 编辑已发布商品(审核中/上架可编辑)   | `ProductPublishDto`   | `{ "code": 200, "msg":"商品修改成功" }`                             |
| 上架/下架商品 | `/api/product/changeStatus/{productId}`        | PUT    | 改变商品状态(1待审核 2上架 3下架)   | `ProductStatusDto`    | `{ "code": 200, "msg":"商品状态修改成功" }`                             |
| 删除商品      | `/api/product/delete/{productId}`              | DELETE | 逻辑删除商品(仅下架商品可删除)   | 无                   | `{ "code": 200, "msg":"商品删除成功" }`                             |
| 我的商品列表  | `/api/product/listMy`                    | GET    | 查看当前卖家的所有商品(分页)   | `page=1&size=10`          | `{ "code": 200, "data":{"total":10, "records":[...]}}`                    |
| 标记商品已售  | `/api/product/sold/{productId}`                | PUT    | 订单完成后标记商品已售   | 无                   | `{ "code": 200, "msg":"商品已标记为已售" }`                             |

### 2.2 买家商品浏览

| 接口名称      | URL                                            | 方法   | 描述           | 请求参数              | 返回示例                                      |
| ------------- | ---------------------------------------------- | ------ | -------------- | --------------------- | --------------------------------------------- |
| 商品详情      | `/api/product/detail/{productId}`              | GET    | 查看商品详情(包含卖家信息)   | 无                   | `{ "code": 200, "data":{"id":1, "title":"", "price":100, "seller":{"nickname":"", "avatar":""}, "images":[...]}}`                  |
| 商品列表      | `/api/product/list`                      | GET    | 查看商品列表(分页+多条件过滤)   | `page=1&size=10&categoryId=1&sort=new`     | `{ "code": 200, "data":{"total":100, "records":[...]}}`                    |
| 按分类浏览    | `/api/product/listCategory/{categoryId}`        | GET    | 按分类查询商品(包含子分类)  | `page=1&size=10`                   | `{ "code": 200, "data":{"total":50, "records":[...]}}`                  |
| 商品数据统计  | `/api/product/stats/{productId}`                | GET    | 查询商品浏览量/收藏量统计 | 无                                                    | `{ "code": 200, "data":{"viewCount": 100, "favoriteCount": 25} }`      |

---

## 4. 订单模块接口

**订单状态定义：** 1=待支付(UNPAID) 2=已支付(PAID) 3=已完成(COMPLETED) 4=已取消(CANCELLED)

| 接口名称     | URL                       | 方法 | 描述                     | 请求参数                                | 返回示例                             |
| ------------ | ------------------------- | ---- | ------------------------ | --------------------------------------- | ------------------------------------ |
| 创建订单     | `/api/order`       | POST | 创建订单(直接购买或从购物车)                 | `OrderCreateDTO`(productId, quantity) | `{ "code": 200, "data": 201 }`    |
| 订单支付     | `/api/order/pay/{orderId}`     | POST | 订单支付(模拟)                 | 无                               | `{ "code": 200, "msg": "支付成功" }` |
| 订单完成     | `/api/order/complete/{orderId}`| POST | 订单完成(卖家发货)                 | 无                               | `{ "code": 200, "msg": "订单完成" }` |
| 买家订单列表 | `/api/order/buyer`        | GET  | 查询买家所有订单(分页+状态筛选)         | `page=1&size=10&status=1`                    | `{ "code": 200, "data":{"total":20, "records":[...]} }`     |
| 卖家订单列表 | `/api/order/seller`       | GET  | 查询卖家所有订单(分页+状态筛选)         | `page=1&size=10&status=2`                    | `{ "code": 200, "data":{"total":15, "records":[...]} }`     |
| 取消订单     | `/api/order/cancel/{orderId}`  | POST | 买家/卖家取消订单(待支付/已支付可取消)        | 无                               | `{ "code": 200, "msg": "取消成功" }` |
| 确认收货     | `/api/order/confirm/{orderId}` | POST | 买家确认收到商品(已发货→已完成)         | 无                               | `{ "code": 200, "msg": "已完成" }`   |
| 查询订单详情 | `/api/order/{orderId}` | GET  | 查询单个订单详情(包含商品信息、卖家信息)         | 无                               | `{ "code": 200, "data":{...} }`   |

### 2.2 买家商品浏览()

| 接口名称     | URL                   | 方法 | 描述             | 请求参数                       | 返回示例                         |
| ------------ | --------------------- | ---- | ---------------- | ------------------------------ | -------------------------------- |
| 商品列表     | `/api/product/list`   | GET  | 分页获取商品列表 | `page, size, sort, categoryId` | `{ "code": 200, "data": [...] }` |
| 商品详情     | `/api/product/{id}`   | GET  | 获取商品详细信息 | `id`                           | `{ "code": 200, "data": {...} }` |
| 按关键词搜索 | `/api/product/search` | GET  | 搜索商品         | `keyword, page, size`          | `{ "code": 200, "data": [...] }` |

---

## 3. 购物车模块接口

| 接口名称         | URL              | 方法   | 描述               | 请求参数              | 返回示例                             |
| ---------------- | ---------------- | ------ | ------------------ | --------------------- | ------------------------------------ |
| 添加商品到购物车 | `/api/cart`      | POST   | 添加商品到购物车           | `{"productId": 1, "quantity": 1}` | `{ "code": 200, "msg": "添加成功" }` |
| 修改购物车商品数量   | `/api/cart/{cartId}` | PUT    | 修改商品购买数量           | `{"quantity": 2}`            | `{ "code": 200, "msg": "修改成功" }` |
| 删除购物车商品   | `/api/cart/{cartId}` | DELETE | 删除购物车中的商品           | 无                  | `{ "code": 200, "msg": "删除成功" }` |
| 查看购物车列表   | `/api/cart/list` | GET    | 查看当前用户购物车 | `page=1&size=10`          | `{ "code": 200, "data":{"total":5, "records":[{"cartId":1, "productId":1, "title":"商品标题", "price":100, "quantity":2}]}}` |
| 批量删除购物车   | `/api/cart/batch` | DELETE | 批量删除购物车商品           | `{"cartIds":[1,2,3]}`                  | `{ "code": 200, "msg": "删除成功" }` |
| 结算购物车       | `/api/cart/checkout` | POST   | 购物车商品结算生成订单(后续使用) | `{"cartIds":[1,2,3], "addressId":1}` | `{ "code": 200, "data":{"totalPrice":500, "totalQuantity":3}}` |

------
---

## 5. 评价与收藏模块

### 5.1 评价接口

| 接口名称     | URL                       | 方法   | 描述               | 请求参数                             | 返回示例                             |
| ------------ | ------------------------- | ------ | ------------------ | ------------------------------------ | ------------------------------------ |
| 创建评价     | `/api/review`             | POST   | 买家评价已完成订单(只能评价一次) | `{"orderId":1, "score":5, "content":"很满意"}` | `{ "code": 200, "msg": "评价成功" }` |
| 查看商品评价 | `/api/review/product/{productId}` | GET    | 查看商品评价列表(分页)   | `page=1&size=10`                         | `{ "code": 200, "data":{"total":50, "records":[...]} }`     |
| 查询是否已评价 | `/api/review/check/{orderId}` | GET    | 检查用户是否已对该订单评价   | 无                         | `{ "code": 200, "data":true}`     |
| 修改评价     | `/api/review/{reviewId}` | PUT    | 修改已发布的评价         | `{"score":4, "content":"一般"}` | `{ "code": 200, "msg": "修改成功" }` |
| 删除评价     | `/api/review/{reviewId}` | DELETE    | 删除已发布的评价         | 无 | `{ "code": 200, "msg": "删除成功" }` |

### 5.2 收藏接口

| 接口名称     | URL                       | 方法   | 描述               | 请求参数                             | 返回示例                             |
| ------------ | ------------------------- | ------ | ------------------ | ------------------------------------ | ------------------------------------ |
| 收藏商品     | `/api/favorite`           | POST   | 添加商品到收藏夹           | `{"productId":1}` | `{ "code": 200, "msg": "收藏成功" }` |
| 取消收藏     | `/api/favorite/{favoriteId}`      | DELETE | 移除收藏的商品           | 无                                 | `{ "code": 200, "msg": "取消成功" }` |
| 查看收藏列表 | `/api/favorite/list`      | GET    | 查看当前用户收藏列表(分页)   | `page=1&size=10`                         | `{ "code": 200, "data":{"total":20, "records":[...]}}`     |
| 检查收藏状态 | `/api/favorite/check/{productId}` | GET    | 检查用户是否收藏该商品        | 无                                 | `{ "code": 200, "data":true}` |

---

## 6. 管理员模块接口

### 6.1 用户管理

| 接口名称      | URL                          | 方法  | 描述                     | 请求参数                         | 返回示例                                         |
| ------------- | ---------------------------- | ----- | ------------------------ | -------------------------------- | ------------------------------------------------ |
| 查看用户列表  | `/api/admin/user/list`           | GET   | 查看所有注册用户(分页+搜索)         | `page=1&size=10&keyword=用户名`            | `{ "code": 200, "data":{"total":100, "records":[...]} }`                 |
| 封禁/解封用户 | `/api/admin/user/status`     | PATCH | 修改用户账号状态(1正常 0封禁)             | `{"userId":1, "status":0}`                 | `{ "code": 200, "msg": "修改成功" }`             |
| 重置用户密码  | `/api/admin/user/password`   | POST  | 重置用户密码(生成临时密码)             | `{"userId":1}`                         | `{ "code": 200, "msg": "密码已重置为: xxx" }`          |
| 查看用户详情  | `/api/admin/user/{userId}`   | GET  | 查看用户详细信息+操作日志             | 无                         | `{ "code": 200, "data":{...} }`          |

### 6.2 商品管理

| 接口名称      | URL                          | 方法  | 描述                     | 请求参数                         | 返回示例                                         |
| ------------- | ---------------------------- | ----- | ------------------------ | -------------------------------- | ------------------------------------------------ |
| 查看全部商品  | `/api/admin/product/list`        | GET   | 管理员查看所有商品(分页+筛选)       | `page=1&size=10&status=2&categoryId=1` | `{ "code": 200, "data":{"total":500, "records":[...]} }`                 |
| 商品审核      | `/api/admin/product/audit`   | PUT  | 审核商品(通过/拒绝)(可选)             | `{"productId":1, "status":2, "reason":""}`              | `{ "code": 200, "msg": "审核完成" }`             |
| 强制下架商品  | `/api/admin/product/offline` | PUT  | 下架违规商品         | `{"productId":1, "reason":"违反规定"}`                      | `{ "code": 200, "msg": "下架成功" }`             |
| 删除商品      | `/api/admin/product/{productId}` | DELETE  | 永久删除商品         | 无                      | `{ "code": 200, "msg": "删除成功" }`             |

### 6.3 订单管理

| 接口名称      | URL                          | 方法  | 描述                     | 请求参数                         | 返回示例                                         |
| ------------- | ---------------------------- | ----- | ------------------------ | -------------------------------- | ------------------------------------------------ |
| 查看平台订单  | `/api/admin/order/list`          | GET   | 查看平台所有订单(分页+筛选)         | `page=1&size=10&status=2&startDate=2026-01-01&endDate=2026-01-31`            | `{ "code": 200, "data":{"total":1000, "records":[...]} }`                 |
| 交易数据统计  | `/api/admin/order/stats`    | GET  | 平台交易数据看板(日/月/年统计)    | `startDate=2026-01-01&endDate=2026-01-31`                 | `{ "code": 200, "data":{"totalOrders": 2000, "totalAmount": 500000, "cancelRate": 0.05} }`             |

### 6.4 分类与标签管理

| 接口名称      | URL                          | 方法  | 描述                     | 请求参数                         | 返回示例                                         |
| ------------- | ---------------------------- | ----- | ------------------------ | -------------------------------- | ------------------------------------------------ |
| 查看分类列表  | `/api/admin/category/list`        | GET   | 查看所有商品分类(树形)         | 无            | `{ "code": 200, "data":[...] }`                 |
| 新增商品分类  | `/api/admin/category`   | POST  | 添加新分类             | `{"name":"手机", "parentId":null}`                 | `{ "code": 200, "data":1 }`             |
| 修改商品分类  | `/api/admin/category/{categoryId}` | PUT  | 编辑分类信息             | `{"name":"二手手机"}`              | `{ "code": 200, "msg": "修改成功" }`             |
| 删除商品分类  | `/api/admin/category/{categoryId}` | DELETE  | 删除分类(需检查关联商品)         | 无                      | `{ "code": 200, "msg": "删除成功" }`             |
| 分类排序      | `/api/admin/category/sort` | POST  | 调整分类显示顺序         | `{"categories":[{"id":1, "sort":1}, {"id":2, "sort":2}]}`                      | `{ "code": 200, "msg": "排序成功" }`             |

---

## 7. 图像识别接口

| 接口名称     | URL                    | 方法 | 描述                                           | 请求参数     | 返回示例                                                     |
| ------------ | ---------------------- | ---- | ---------------------------------------------- | ------------ | ------------------------------------------------------------ |
| 图像识别上传 | `/api/image/recognize` | POST | 上传商品图片，自动识别类别、标签、标题和参考价格 | `image(multipart)` | `{ "code": 200, "data":{"category":"电子产品", "suggestedTags":["二手手机","苹果"], "suggestedTitle":"二手苹果 iPhone 13", "suggestedPrice":3500} }` |

---

## 📌 返回格式说明

所有接口统一返回格式：

```json
{
  "code": 200,                    // 状态码: 200成功, 其他值为失败
  "msg": "成功/错误信息",          // 提示信息
  "data": null|{...}|[...]        // 具体返回数据
}
```

## 🔐 认证说明

- **登录后获取Token** → `/api/auth/login` 返回的 `token` 字段
- **请求头中携带Token** → `Authorization: Bearer {token}`
- **管理员接口** → 需要 `role = 1` 的用户权限

## ⚠️ 常用HTTP状态码

| 状态码 | 含义     | 说明                       |
| ------ | -------- | -------------------------- |
| 200    | 成功     | 请求成功                   |
| 400    | 请求错误 | 参数错误或业务异常         |
| 401    | 未授权   | Token缺失或过期            |
| 403    | 禁止访问 | 权限不足                   |
| 404    | 不存在   | 资源不存在                 |
| 500    | 服务器错误 | 系统异常                  |
