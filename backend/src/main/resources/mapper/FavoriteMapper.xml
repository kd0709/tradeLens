<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cjh.backend.mapper.FavoriteMapper">

    <resultMap id="BaseResultMap" type="com.cjh.backend.entity.Favorite">
            <id property="id" column="id" jdbcType="BIGINT"/>
            <result property="userId" column="user_id" jdbcType="BIGINT"/>
            <result property="productId" column="product_id" jdbcType="BIGINT"/>
            <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,user_id,product_id,
        create_time
    </sql>

    <select id="selectFavoriteListByUserId" resultType="com.cjh.backend.dto.Favorite.FavoriteListDto">
        SELECT
            f.id,
            f.product_id,
            p.title,
            p.price,
            (SELECT image_url FROM tradelens.product_image WHERE product_id = f.product_id ORDER BY sort ASC LIMIT 1) AS image_url,
            f.create_time
        FROM tradelens.favorite f
                 LEFT JOIN tradelens.product p ON f.product_id = p.id
        WHERE f.user_id = #{userId}
        ORDER BY f.create_time DESC
    </select>

</mapper>
